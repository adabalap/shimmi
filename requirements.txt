# Shimmi v3.0.3 — Python requirements
# Python >= 3.12 required.
#
# Install (inside venv recommended):
#   pip install -r requirements.txt
#
# NOTE: sentence-transformers pulls in PyTorch (~2 GB).
# For a CPU-only server, install torch first:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   then: pip install -r requirements.txt

# Web framework
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.30.0,<1.0
python-multipart>=0.0.12

# LLM providers
groq>=0.12.0                  # Groq async client (primary)
# Google Gemini uses the OpenAI-compatible REST endpoint via httpx.
# No extra SDK — just set GEMINI_API_KEY in .env.

# HTTP
httpx>=0.27.0,<1.0            # MCP client + Gemini REST calls
anyio>=4.4.0

# Database
aiosqlite>=0.20.0
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Validation
pydantic>=2.7.0,<3.0

# Configuration
python-dotenv>=1.0.0

# Timezone
tzdata>=2024.1

# MCP sidecar (mcp_server.py — same or separate venv)
yfinance>=0.2.40
gnews>=0.3.7
